{
  "name": "html-form-confirm",
  "type": "module",
  "version": "0.0.1",
  "description": "Merge DOM elements for html-form",
  "main": "html-form-confirm.js",
  "homepage": "https://github.com/jon49/html-form-confirm#readme",
    "bugs": {
        "url": "https://github.com/jon49/html-form-confirm/issues"
    },
  "scripts": {
    "start": "esbuild html-form-confirm.js --format=iife --bundle --outdir=public && esbuild ./public/html-form-confirm.js --minify --outfile=public/html-form-confirm.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "htmf",
    "html-form",
    "html",
    "merge"
  ],
  "author": "Jon Nyman",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.25.10"
  }
}
